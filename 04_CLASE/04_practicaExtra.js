// aterrizar avion

/* let velocidadDeVuelo = 267;
let alturaDevuelo = 300;

if ((268 <= velocidadDeVuelo) && (velocidadDeVuelo <= 278) && (150 <= alturaDevuelo ) && (alturaDevuelo <= 300)) {
    console.log("altura de vuelo " + alturaDevuelo)
    console.log("velocidad de vuelo " + velocidadDeVuelo)
    console.log(" ")
    console.log("el avion esta listo para aterrizar")
} else {
    console.log("altura de vuelo " + alturaDevuelo)
    console.log("velocidad de vuelo " + velocidadDeVuelo)
    console.log(" ")
    console.log("la velocidad de vuelo o la altura de vuelo no es correcta")
} */

/* let velocidadDeVuelo = 267;
let alturaDevuelo = 300;

let isAlturaOk = (150 <= alturaDevuelo ) && (alturaDevuelo <= 300);
let isVelocidadOk = (268 <= velocidadDeVuelo) && (velocidadDeVuelo <= 278);


if ( isVelocidadOk && isAlturaOk) {
    console.log("altura de vuelo " + alturaDevuelo)
    console.log("velocidad de vuelo " + velocidadDeVuelo)
    console.log(" ")
    console.log("el avion esta listo para aterrizar")
} else {
    if ( !isVelocidadOk ){
        console.log("corregir la velocidad " + velocidadDeVuelo);
    }

    if ( !isAlturaOk){
        console.log("corregir la altura " + alturaDevuelo);
    }

    console.log("altura de vuelo " + alturaDevuelo)
    console.log("velocidad de vuelo " + velocidadDeVuelo)
    console.log(" ")
    console.log("la velocidad de vuelo o la altura de vuelo no es correcta")
} */